# Ramez Sameh Portfolio

## الفكرة البصرية

تم بناء الواجهة بأسلوب **editorial developer**: خلفية داكنة عميقة، لمسات indigo/cyan، شبكة نقطية خفيفة، خط Space Grotesk للعناوين وخط DM Sans للنصوص، مع حركات دخول قصيرة عبر Framer Motion. الهدف هو تقديم البورتفوليو كمنتج تقني متماسك، وليس كقائمة سيرة ذاتية تقليدية.

## هيكل المجلدات

```text
client/
├── index.html
└── src/
    ├── App.tsx                 # BrowserRouter ومسارات الموقع
    ├── main.tsx                # نقطة تشغيل React
    ├── index.css               # التصميم العام، الألوان، الاستجابة والحركات
    ├── data/
    │   └── constants.ts        # البيانات النصية للمعلومات والمشاريع والمهارات
    ├── components/
    │   ├── Layout.tsx          # الغلاف العام + Navbar + Footer
    │   ├── Navbar.tsx          # التنقل المكتبي وقائمة الهاتف
    │   ├── Footer.tsx          # التذييل وCTA
    │   ├── ProjectCard.tsx     # بطاقة المشروع وجلب عدد النجوم من GitHub API
    │   ├── SectionHeading.tsx  # عنوان موحد للأقسام الداخلية
    │   └── Reveal.tsx           # حركة الظهور عند الوصول للقسم
    └── pages/
        ├── Home.tsx            # Hero، نبذة، المشاريع المختارة، المبادئ
        ├── Projects.tsx        # صفحة المشاريع الثلاثة
        ├── Skills.tsx          # شبكة المهارات ومؤشرات التقدم
        ├── Contact.tsx         # نموذج التواصل عبر mailto
        └── NotFound.tsx         # صفحة 404
```

## شرح الأجزاء الأساسية

- **`App.tsx`** يستخدم `react-router-dom` لتوفير المسارات `/` و`/projects` و`/skills` و`/contact` مع مسار بديل لصفحة 404.
- **`Layout.tsx`** يضع الـ Navbar والـ Footer حول جميع الصفحات، ويعيد التمرير إلى أعلى الصفحة عند تغيير المسار.
- **`constants.ts`** هو المكان الوحيد الذي يحتاج إلى التعديل عادةً عند تغيير الاسم أو الوصف أو رابط GitHub أو المشاريع أو المهارات.
- **`ProjectCard.tsx`** يعرض بيانات كل مستودع، ويجرب جلب عدد النجوم الحالي من GitHub API مع قيمة ثابتة احتياطية في حال فشل الطلب أو الوصول إلى حد API.
- **`Contact.tsx`** لا يخزن البيانات؛ عند الإرسال يفتح تطبيق البريد الافتراضي باستخدام `mailto:`. يمكن استبداله لاحقاً بـ Formspree أو EmailJS.
- **`index.css`** يحتوي على نظام الألوان، الـ typography، البطاقات، الـ progress bars، تخطيط الهاتف، و`prefers-reduced-motion`.

## التشغيل

```bash
pnpm install
pnpm dev
```

وللتحقق من النسخة الإنتاجية:

```bash
pnpm exec tsc --noEmit
pnpm run build
```

## أماكن التخصيص السريعة

1. عدّل `personal.email` داخل `client/src/data/constants.ts` لأن البريد الوارد في التصميم افتراضي وقابل للاستبدال.
2. أضف أو احذف مشروعاً من مصفوفة `projects`.
3. غيّر نسب المهارات من الخاصية `level`.
4. غيّر الخطوط والألوان الأساسية من أعلى `client/src/index.css`.
5. يمكن لاحقاً استبدال `mailto:` في `Contact.tsx` بخدمة إرسال فعلية.

## التحقق المنفذ

- `pnpm exec tsc --noEmit` — ناجح.
- `pnpm run build` — ناجح.
- تم فحص صفحات Home وProjects وSkills وContact على سطح المكتب والهاتف.
- تم التحقق من القائمة المحمولة، المسارات، البطاقات، النموذج، والروابط الخارجية.

## زر تحميل السيرة الذاتية

تم تحديث البريد إلى `samehramez24@gmail.com` في جميع مواضع التواصل، كما أصبح زر **View / Download CV** في الـ Hero يفتح السيرة الذاتية الحقيقية من Google Drive في تبويب جديد. تم التحقق من استجابة الرابط بنجاح.

## صور المشاريع

تم إنشاء ثلاث لقطات واجهة مخصصة بأسلوب dark SaaS موحد، ثم رفعها إلى WebDev Storage وربطها من خلال خاصية `image` داخل `client/src/data/constants.ts`. تستخدم `ProjectCard` الصور مع طبقة تباين، وتحميل lazy، وتكبير خفيف عند التحويم، مع الحفاظ على أبعاد مناسبة للشاشات الصغيرة.

## صفحات تفاصيل المشاريع ومعارض الصور

تمت إضافة مسار ديناميكي `/projects/:projectId` وصفحة `ProjectDetail.tsx` مستقلة لكل مشروع. تتيح الصفحة استعراض معرض من صورتين لكل مشروع عبر صور مصغرة وأزرار السابق/التالي، مع عرض الملخص، التقنيات، رابط المستودع، وزر العودة إلى صفحة المشاريع. أصبحت بطاقات المشاريع قابلة للنقر ولوحة المفاتيح لفتح صفحة التفاصيل، مع إبقاء رابط GitHub منفصلاً.

## النسخة العربية والصورة الشخصية

تمت إضافة `LanguageContext` مع زر تبديل عالمي بين العربية والإنجليزية، وتحديث `lang` و`dir` تلقائيًا لدعم RTL مع حفظ اختيار المستخدم في `localStorage`. تمت ترجمة التنقل والصفحة الرئيسية والمشاريع والمهارات وصفحة التواصل وتفاصيل المشاريع، مع إضافة حقول عربية لأسماء ووصف المشاريع. كما تم رفع الصورة الشخصية المرفقة واستخدامها داخل الـ Hero في الصفحة الرئيسية.
